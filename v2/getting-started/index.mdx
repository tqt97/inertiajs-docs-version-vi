---
title: Giới thiệu
---

import V2UpgradeWarning from "/snippets/v2-upgrade-warning.mdx";

<V2UpgradeWarning />

Inertia là một cách tiếp cận mới để xây dựng ứng dụng web truyền thống do máy chủ điều khiển. Chúng tôi gọi mô hình này là modern monolith.

Inertia cho phép bạn tạo ứng dụng một trang được render hoàn toàn ở phía client mà không phải gánh sự phức tạp thường đi kèm SPA hiện đại. Inertia làm được điều đó bằng cách tận dụng các pattern phía máy chủ quen thuộc mà bạn vốn đã sử dụng.

Inertia không có routing phía client và cũng không yêu cầu API. Bạn chỉ cần xây dựng controller và page view như cách vẫn làm trước đây. Inertia hoạt động tốt với mọi backend framework, nhưng được tối ưu đặc biệt cho [Laravel](https://laravel.com).

## Không phải một framework

Inertia không phải framework, cũng không thay thế framework phía server hoặc phía client hiện có của bạn. Thay vào đó, nó được thiết kế để làm việc cùng các framework đó. Có thể xem Inertia như lớp keo nối hai phía với nhau. Inertia thực hiện điều này thông qua adapter. Hiện có ba client-side adapter chính thức (React, Vue và Svelte) cùng bốn server-side adapter (Laravel, Rails, Phoenix và Django).

## Bước tiếp theo

Muốn tìm hiểu thêm một chút trước khi bắt đầu? Hãy xem các trang [phù hợp với ai](/v2/core-concepts/who-is-it-for) và [cách hoạt động](/v2/core-concepts/how-it-works). Hoặc nếu đã sẵn sàng, hãy đi thẳng đến [hướng dẫn cài đặt](/v2/installation/server-side-setup).

---

## Tài liệu chính thức

Bài dịch này được đối chiếu từ [tài liệu Inertia.js v2 chính thức](https://inertiajs.com/docs/v2/getting-started). Nếu có khác biệt do phiên bản hoặc cập nhật mới, hãy ưu tiên tài liệu chính thức làm nguồn tham chiếu.
